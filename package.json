{
  "name": "alex_a_montreal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "retro-portfolio build",
    "postbuild": "cp config-source.json dist/ && cp CNAME dist/ 2>/dev/null || true",
    "dev": "retro-portfolio dev",
    "admin": "retro-portfolio admin",
    "sync": "retro-portfolio sync",
    "start": "npm run build && npm run dev & npm run admin",
    "deploy": "retro-portfolio deploy",
    "validate": "retro-portfolio validate"
  },
  "dependencies": {
    "@mtldev514/retro-portfolio-maker": "^1.0.32"
  }
}
